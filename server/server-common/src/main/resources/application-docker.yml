# Docker部署环境
server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATSASOURCE_PASSWORD:island_root_2024}
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: island_redis_2024

jwt:
  secret: ${JWT_SECRET:island-campus-jwt-secret-key-2024-super-long-secret-for-hmac-sha256!!}

weather:
  default-api-key: ${WEATHER_API_KEY:uapi-j5jky0ggkSIABn-OreTfovNhfyEPDVRewQ3BgEBQ}

logging:
  level:
    com.islandcampus: info
